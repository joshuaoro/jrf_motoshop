# ============================================================
# JRF Motorcycle Parts - Development / CI dependencies
# Install with:  pip install -r requirements-dev.txt
# ============================================================
-r requirements.txt

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
pytest-mock==3.14.0

# --- Browser tests (tests/browser) ---
# Needs a one-off browser download:  python -m playwright install chromium
playwright==1.47.0
pytest-playwright==0.5.2

# --- Test database driver (tests run on in-memory SQLite) ---
# (sqlite3 ships with CPython - no driver needed)

# --- Linting / formatting / typing ---
black==24.8.0
flake8==7.1.1
mypy==1.11.2
